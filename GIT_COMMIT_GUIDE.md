# Инструкция по загрузке проекта в Git

## ✅ Проверка безопасности

Перед коммитом убедитесь, что:
- ✅ `.env` файлы в `.gitignore` (API ключи не попадут в репозиторий)
- ✅ `node_modules/` в `.gitignore`
- ✅ API ключ не найден в коде

## 📝 Команды для коммита

### 1. Добавить файлы проекта

```bash
cd "/Users/nurgazyzhangozy/Desktop/IOS DEVELOPER/news-app-frontend-test"
git add mobile/ web/ README.md .gitignore
```

### 2. Проверить, что добавлено

```bash
git status
```

### 3. Создать коммит

```bash
git commit -m "feat: реализовано новостное приложение

- Mobile приложение (React Native + Expo)
  - Биометрическая аутентификация (Touch ID/Face ID)
  - Список новостей с поиском и фильтрацией
  - Детальный просмотр статей с WebView
  - Избранное с локальным хранением
  - Push-уведомления
  - Работа с файлами (отправка/скачивание)

- Web приложение (Next.js)
  - Аутентификация
  - Список новостей с поиском и фильтрацией
  - Детальный просмотр
  - Избранное

- Архитектура Feature-Sliced Design
- Redux Toolkit + RTK Query
- TypeScript
- Тесты (Jest)
- Фильтрация по категориям, странам, сортировка"
```

### 4. Отправить в удаленный репозиторий

```bash
git push origin master
```

## ⚠️ Важно

Если у вас еще нет удаленного репозитория на GitHub:

1. Создайте репозиторий на GitHub
2. Добавьте remote:
   ```bash
   git remote add origin https://github.com/ваш-username/название-репозитория.git
   ```
3. Отправьте код:
   ```bash
   git push -u origin master
   ```

## 🔒 Безопасность

- API ключи находятся в `.env` файлах, которые в `.gitignore`
- Не коммитьте `.env` файлы
- В README указано, как получить API ключ

