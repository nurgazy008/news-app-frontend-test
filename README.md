# News App - Тестовое задание Frontend-разработчика

Кроссплатформенное мобильное приложение для просмотра новостей с использованием React Native и веб-версия на Next.js.

## 📱 Описание проекта

Приложение для просмотра новостных статей с возможностью:
- Просмотра списка новостей с фильтрацией и поиском
- Детального просмотра статей через WebView
- Добавления статей в избранное
- Биометрической аутентификации (Touch ID / Face ID)
- Push-уведомлений
- Отправки и скачивания файлов

## 🏗️ Архитектура

Проект использует **Feature-Sliced Design (FSD)** архитектуру:

```
src/
├── app/          # Инициализация приложения, провайдеры, роутинг
├── pages/        # Страницы приложения
├── widgets/      # Крупные составные UI-компоненты
├── features/     # Бизнес-фичи приложения
├── entities/     # Бизнес-сущности
└── shared/       # Переиспользуемые модули (UI, утилиты, API)
```

## 🛠️ Технологический стек

### Mobile (React Native)
- **React Native** с Expo
- **TypeScript**
- **Redux Toolkit** + **RTK Query** для управления состоянием
- **React Navigation** для навигации
- **React Native WebView** для просмотра статей
- **AsyncStorage** для локального хранения
- **React Native Biometrics** для биометрической аутентификации
- **Expo Notifications** для push-уведомлений
- **Expo File System** и **Expo Document Picker** для работы с файлами

### Web (Next.js)
- **Next.js 16** с App Router
- **TypeScript**
- **Tailwind CSS** для стилизации
- **Shadcn/ui** компоненты
- **Redux Toolkit** + **RTK Query** для управления состоянием

## 📦 Установка и запуск

### Требования
- Node.js 18+ 
- npm или yarn
- Для iOS: Xcode и CocoaPods
- Для Android: Android Studio

### Mobile приложение

1. **Перейдите в папку mobile:**
   ```bash
   cd mobile
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Запустите приложение:**
   ```bash
   # Запуск Expo
   npm start
   
   # Или для конкретной платформы
   npm run ios      # iOS симулятор
   npm run android  # Android эмулятор
   npm run web      # Веб-версия
   ```

4. **Для iOS (если используете bare React Native):**
   ```bash
   cd ios
   pod install
   cd ..
   npm run ios
   ```

### Web приложение

1. **Перейдите в папку web:**
   ```bash
   cd web
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Запустите dev-сервер:**
   ```bash
   npm run dev
   ```

4. **Откройте в браузере:**
   ```
   http://localhost:3000
   ```

## 🎯 Функциональные требования

### ✅ Реализовано

- [x] Структура проекта по Feature-Sliced Design
- [x] Настройка Redux + RTK Query для mobile и web
- [x] Главный экран (список новостей)
  - [x] Загрузка данных из NewsAPI
  - [x] Отображение карточек новостей
  - [x] Бесконечная прокрутка (пагинация)
  - [x] Поиск по заголовкам
  - [x] Индикатор загрузки
  - [x] Обработка ошибок
- [x] Детальный экран статьи
  - [x] Полная информация о новости
  - [x] WebView для просмотра статьи (mobile)
  - [x] Кнопка добавления в избранное
  - [x] Навигация назад
- [x] Экран избранного
  - [x] Список сохраненных статей
  - [x] Удаление из избранного
  - [x] Локальное хранение (AsyncStorage для mobile, localStorage для web)
- [x] Биометрическая аутентификация (mobile)
  - [x] Touch ID / Face ID
  - [x] Экран входа
  - [x] Функция logout
- [x] Push-уведомления (mobile)
  - [x] Настройка Expo Notifications
  - [x] Уведомления о новых новостях
- [x] Работа с файлами (mobile)
  - [x] Отправка файлов с устройства
  - [x] Скачивание файлов
  - [x] Отображение прогресса
- [x] Веб-версия (Next.js)
  - [x] Аутентификация
  - [x] Список новостей
  - [x] Детальный просмотр
  - [x] Избранное
- [x] Фильтрация новостей
  - [x] Фильтр по категориям
  - [x] Фильтр по странам
  - [x] Сортировка (по дате, популярности, релевантности)
- [x] Тестирование
  - [x] Юнит-тесты для Redux слайсов
  - [x] Настроена инфраструктура тестирования (Jest)

## 📁 Структура проекта

```
news-app-frontend-test/
├── mobile/                 # React Native приложение
│   ├── src/
│   │   ├── app/           # Инициализация, провайдеры
│   │   ├── pages/         # Страницы
│   │   ├── widgets/       # Виджеты
│   │   ├── features/      # Фичи
│   │   ├── entities/      # Сущности
│   │   └── shared/        # Общие модули
│   ├── App.tsx
│   └── package.json
│
├── web/                    # Next.js приложение
│   ├── src/
│   │   ├── app/           # Next.js App Router
│   │   ├── pages/         # Страницы
│   │   ├── widgets/       # Виджеты
│   │   ├── features/      # Фичи
│   │   ├── entities/      # Сущности
│   │   └── shared/        # Общие модули
│   └── package.json
│
└── README.md
```

## 🔧 Настройка API

### NewsAPI

1. Получите бесплатный API ключ на [newsapi.org](https://newsapi.org/)
2. Для mobile приложения создайте файл `.env` в папке `mobile`:
   ```
   EXPO_PUBLIC_NEWS_API_KEY=your_api_key_here
   ```
3. Для web приложения создайте файл `.env.local` в папке `web`:
   ```
   NEXT_PUBLIC_NEWS_API_KEY=your_api_key_here
   ```

**Важно:** Без API ключа приложение не сможет загружать новости. Бесплатный план NewsAPI позволяет делать до 100 запросов в день.

## 📝 Коммиты

Проект следует принципам Git Flow:
- `feat/` - новая функциональность
- `fix/` - исправление багов
- `refactor/` - рефакторинг
- `docs/` - документация
- `style/` - стилизация
- `test/` - тесты

## 🚀 Быстрый старт

### Mobile приложение

```bash
cd mobile
npm install
# Создайте .env файл с EXPO_PUBLIC_NEWS_API_KEY
npm start
```

### Web приложение

```bash
cd web
npm install
# Создайте .env.local файл с NEXT_PUBLIC_NEWS_API_KEY
npm run dev
```

## 📱 Особенности реализации

### Mobile (React Native)
- Использует Expo для кроссплатформенной разработки
- Биометрическая аутентификация через `react-native-biometrics`
- Push-уведомления через `expo-notifications`
- Работа с файлами через `expo-file-system` и `expo-document-picker`
- WebView для просмотра статей внутри приложения
- Локальное хранение через `AsyncStorage`

### Web (Next.js)
- Использует Next.js 16 с App Router
- Клиентские компоненты для интерактивности
- Локальное хранение через `localStorage`
- Адаптивный дизайн с Tailwind CSS

## 🧪 Тестирование

```bash
# Mobile
cd mobile
npm test

# Web
cd web
npm test
```

## 📄 Лицензия

Этот проект создан в рамках тестового задания.

## 👤 Автор

Nurgazy Zhangozy

